<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:GoldenBook;assembly=GoldenBook"
             x:Class="GoldenBook.Views.AdvertisersFormPage"
             Title="Saisie d'un sponsor"
             BindingContext="{Binding Source={x:Static local:App.Locator}, Path=AdvertisersForm}">

  <ScrollView>
    <StackLayout VerticalOptions="Start" Padding="10">
      <StackLayout Padding="0,0,0,10">
        <Entry x:Name="firstNameEntry" Placeholder="Prénom du sponsor" Keyboard="Text" />
        <Entry x:Name="lastNameEntry" Placeholder="Nom du sponsor" Keyboard="Text" />
        <Entry x:Name="emailEntry" Placeholder="Adresse e-mail du sponsor" Keyboard="Email" />
        <Entry x:Name="amountEntry" Placeholder="Montant du soutien" Keyboard="Numeric" />
        <Editor x:Name="messageEditor" Text="Message" Keyboard="Text" />
      </StackLayout>

      <StackLayout Padding="0,0,0,10">
        <Label Text="Ajouté par :" />
        <Picker x:Name="proposersPicker" />
      </StackLayout>

      <StackLayout Padding="0">
        <Button Text="Prendre une photo" Command="{Binding TakePictureCommand}" />
        <Button Text="Envoyer" Clicked="OnSendButton_Clicked" />
        <Image Source="{Binding ImageSource}" />
      </StackLayout>
    </StackLayout>
  </ScrollView>

</ContentPage>